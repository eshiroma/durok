var Model = function(domain, startDate, endDate) {
  var init = function();
  var setData = function(domain, startDate, endDate);

  var getPlayers = function();
  var getPlayerGames = function(playerId);
  var getGames = function();
  var getGameData = function(gameId);

  // var addPlayer = function(playerName);
  // var addGame = function(date, domain, playerIds, durokId);
};
